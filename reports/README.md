# Runtime reports

Reports are generated runtime artifacts. The directory is kept only as a
documented output location; generated reports, timestamped diagnostics, maps,
large evidence payloads, and downloaded validation caches are ignored by Git.

Use `fixtures/demo/` for intentionally curated, deterministic demonstration
artifacts. Do not copy arbitrary `latest_*` output into the fixture tree.
